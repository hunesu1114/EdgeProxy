# edge 프록시 설정 템플릿.
#
#   cp .env.example .env
#
# 대부분 기본값 그대로 두면 된다. 운영 서버에서는 이 파일 없이도 동작한다.

# 인증서 루트. 하위에 도메인별 디렉터리를 둔다 (default/, filehub/, boram/)
CERT_DIR=/etc/edge/certs

# 공개 포트. 운영은 기본값(80/443)을 쓴다.
# 로컬에서 검증할 때만 겹치지 않는 포트로 바꾼다.
HTTP_PORT=80
HTTPS_PORT=443
